[package]
name = "easyplanner-app"
version = "0.1.0"
edition = "2024"

[dependencies]
boltffi = { workspace = true }
easyplanner = { path = "../easyplanner", features = ["storage-sqlite"] }

[lib]
crate-type = ["cdylib", "staticlib"]
