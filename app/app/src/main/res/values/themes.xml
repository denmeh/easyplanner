<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.EasyPlanner" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Shown from process start until the first app frame; avoids a blank window during Compose / JNI init. -->
    <style name="Theme.EasyPlanner.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.EasyPlanner</item>
    </style>
</resources>
